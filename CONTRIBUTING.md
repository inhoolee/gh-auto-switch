# Contributing

Thanks for helping improve `gh-auto-switch`.

## Development

Requirements:

- Go 1.22+

Commands:

```sh
make test
make build
```

## Pull Requests

- Keep changes focused and small when possible.
- Include tests when changing parsing, matching, or config behavior.
- Avoid introducing dependencies unless there is a clear win (startup time matters for shell hooks).

